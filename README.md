npm install prisma --save-dev
npx prisma
npx prisma init


-- first create all users
---- user create project and generate the API KEYs
---- want to work on the store the logs and retrieve more compatibly.